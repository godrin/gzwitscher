<h1>Listing messages of <%=@user.email%></h1>

<table>
  <tr>
    <th>Text</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @messages.each do |message| %>
  <tr>
    <td><%= message.text %></td>
    <td><%= message.user_id %></td>
    <td><%= link_to 'Show', [@user,message] %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Message', new_user_message_path(current_user.id) %>
